<Window x:Name="Btn_downloadMultiple" x:Class="Dynamics365CustomizingDownloader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctr="clr-namespace:Dynamics365CustomizingDownloader.Controls"
        mc:Ignorable="d"
        Title="Dynamics Customizing Downloader" Height="350" Width="525">
    <Grid>
        <ComboBox x:Name="cbx_connection" HorizontalAlignment="Left" Margin="12,66,0,0" VerticalAlignment="Top" Width="179" SelectionChanged="Cbx_connection_SelectionChanged"/>
        <Label Content="Connection:" HorizontalAlignment="Left" Margin="12,40,0,0" VerticalAlignment="Top"/>
        <Button x:Name="btn_download" Content="Download" HorizontalAlignment="Left" Margin="196,67,0,0" VerticalAlignment="Top" Width="309" Click="Button_Click" />
        <ctr:LoadingPanel x:Name="loadingPanel"
                        IsLoading="{Binding PanelLoading}"
                        Message="{Binding PanelMainMessage}"
                        SubMessage="{Binding PanelSubMessage}" 
                        ClosePanelCommand="{Binding PanelCloseCommand}" Margin="0,0,-0.333,-0.333" Foreground="#FF494949" Height="83" VerticalAlignment="Bottom" />
        <Menu Height="21" VerticalAlignment="Top" Margin="0,0,-0.333,0" IsMainMenu="True">
            <MenuItem Header="_File">

                <MenuItem x:Name="Cbx_DebuggingEnabled" Header="Enable Debugging" IsCheckable="True" Click="Cbx_DebuggingEnabled_Click"  />
                <Separator />
                <MenuItem Header="_Exit" Click="MenuItemExit_Click" />
            </MenuItem>

            <MenuItem Header="_Edit">
                <MenuItem Header="_Cut" Command="Cut">
                </MenuItem>
                <MenuItem Header="_Copy" Command="Copy">
                </MenuItem>
                <MenuItem Header="_Paste" Command="Paste">
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Info">
                <MenuItem Header="_About" Click="MenuItemAbout_Click"></MenuItem>
                <MenuItem Header="_Updates" Click="MenuItemUpdate_Click"></MenuItem>
            </MenuItem>
        </Menu>
        <DataGrid x:Name="Dtg_Solutions" Margin="10,101,13.667,87.667" ItemsSource="{Binding}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Binding="{Binding DownloadIsChecked}"  Header=""/>
                <DataGridTextColumn Binding="{Binding UniqueName}" Header="Unique Name" IsReadOnly="True"></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Name}" Header="Solution Name" IsReadOnly="True"></DataGridTextColumn>

            </DataGrid.Columns>
        </DataGrid>

    </Grid>

</Window>
